#🔭AddTags[position=9][color=#B7A7DB]

Below are some nested tags I created in Zotero, they start with # and are connected with /:
```js
Zotero.ZoteroGPT.views.messages = [];
JSON.stringify(Zotero.ZoteroStyle.data.views.tagsUI.plainTags.slice(0, 20))
```
---
This is the abstract of a document:
```js
ZoteroPane.getSelectedItems()[0].getField("abstractNote")
```
---

Please help me analyze what nested tags I should add to this document based on my existing nested tags. Please recommend three tags and give me a code snippet that will be executed in Zotero. Here is the sample code:  
```js
const tags = ["#tag1/tag2/tag3", "#tag1/tag2/tag3", "#tag1/tag2/tag3"]
item = ZoteroPane.getSelectedItems()[0]
tags.forEach(tag=>{
  item.addTag(tag)
})
await item.saveTx()
```
---
You only need to give me the code, no text description is needed besides the code. The code you provide is: