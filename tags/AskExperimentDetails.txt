#Summarize Experiment Methods[pos=9]

Please extract information from this paragraph in bullet point format, including: number of subjects, experimental design (e.g., 2(Gender: male vs female) X 2(Matching Condition: matching or mismatching)), experimental procedure (e.g., subjects first view stimuli, then respond, and finally receive feedback). Please answer in English.  
Below is the methods section of the paper:  
```js
const ztoolkit = Zotero.ZoteroGPT.data.ztoolkit
let getSelection = () => {
    return ztoolkit.Reader.getSelectedText(
 Zotero.Reader.getByTabID(Zotero_Tabs.selectedID)
    );
}
ztoolkit.log(getSelection())
getSelection()
```

---

When PDF text is selected